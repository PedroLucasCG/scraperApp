############################
# Frontend (Vite)
############################

# Base URL of your API (must include /api)
# Match PORT of the backend if running locally
VITE_API_BASE=http://localhost:8082/api
